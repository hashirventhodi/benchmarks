version: "2"
sql:
  - engine: postgresql
    queries: queries.sql
    schema: ../../schema.sql
    gen:
      go:
        package: dbq
        out: dbq
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
